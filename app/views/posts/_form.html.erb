<%= form_for @post do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :author %>
  <%= f.text_field :author, value: @user.username, readonly: true %>

  <%= f.label :content %>
  <%= f.text_area :content, :size => "70x30", :wrap => "hard" %>

  <%= f.submit %>
<% end %>
